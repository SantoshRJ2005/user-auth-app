<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Approved Rides | SharingYatra</title>
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        /* --- Root Variables --- */
        :root {
            --primary-color: #ff6b4a;
            --secondary-color: #2c3e50;
            --bg-light: #f7f8fc;
            --text-dark: #333;
            --text-light: #667;
            --white: #ffffff;
            --border-color: #dfe6f0;
            --success: #28a745;
            --danger: #dc3545;
            --info: #007bff;
            --warning: #ffc107; 
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            font-family: 'Poppins', sans-serif;
            display: flex;
            background: var(--bg-light);
        }

        /* --- Sidebar (Updated with Toggle Responsive CSS) --- */
        .sidebar {
            width: 250px;
            height: 100vh;
            background: var(--secondary-color);
            padding: 30px 20px;
            color: white;
            position: fixed;
            top: 0;
            left: 0;
            display: flex;
            flex-direction: column;
            z-index: 1000;
        }
        .sidebar h2 { /* Changed from .logo */
            color: var(--primary-color);
            font-size: 26px;
            font-weight: 700;
            margin-bottom: 35px;
            padding-left: 5px;
            /* text-align: center; */ /* Removed to match other pages */
        }
        
        .menu-toggle {
            display: none;
            background: none;
            border: none;
            color: var(--white);
            font-size: 1.6rem;
            cursor: pointer;
        }

        .sidebar-links {
            display: flex;
            flex-direction: column;
            flex: 1; /* This makes it fill the space */
        }

        .sidebar-links nav { flex: 1; }

        .sidebar a {
            display: flex;
            align-items: center;
            gap: 12px;
            color: white;
            text-decoration: none;
            padding: 12px 10px;
            margin-bottom: 10px;
            border-radius: 8px;
            transition: all 0.3s ease;
        }
        .sidebar a i { 
            width: 20px; 
            text-align: center;
            font-size: 0.95rem;
        }
        .sidebar a:hover,
        .sidebar a.active {
            /* Changed to match other pages */
            color: var(--white);
            background: var(--primary-color);
        }
        .sidebar a.logout { 
            margin-top: 20px; /* Pushes from nav */
        }

        /* --- Main Content --- */
        .main {
            flex: 1;
            padding: 30px;
            margin-left: 250px;
        }
        .main h1 {
            margin-bottom: 25px;
            color: var(--secondary-color);
            font-size: 2.2rem;
            font-weight: 700;
            padding-bottom: 15px;
            border-bottom: 1px solid var(--border-color);
        }

        /* --- New: Date Filter Bar --- */
        .filter-bar {
            background: var(--white);
            padding: 20px;
            margin-bottom: 25px;
            border-radius: 12px;
            border: 1px solid var(--border-color);
            box-shadow: 0 5px 15px rgba(0,0,0,0.03);
            display: flex;
            align-items: center;
            flex-wrap: wrap; /* Allows wrapping on small screens */
            gap: 15px;
        }
        .filter-group {
            display: flex;
            flex-direction: column;
            gap: 5px;
        }
        .filter-group label {
            font-size: 0.8rem;
            font-weight: 600;
            color: var(--text-light);
            text-transform: uppercase;
        }
        .filter-group input[type="date"] {
            padding: 8px 12px;
            border: 1px solid var(--border-color);
            border-radius: 8px;
            font-family: 'Poppins', sans-serif;
            font-size: 0.9rem;
            color: var(--text-dark);
        }
        .filter-buttons {
            display: flex;
            gap: 10px;
            margin-top: auto; /* Aligns buttons to bottom if inputs wrap */
        }
        .btn {
            padding: 9px 18px;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            font-size: 0.9rem;
            transition: all 0.3s ease;
            text-decoration: none; /* For 'Clear' link */
            display: inline-block;
        }
        .btn-filter {
            background-color: var(--primary-color);
            color: white;
        }
        .btn-filter:hover {
            opacity: 0.9;
        }
        .btn-clear {
            background-color: var(--text-light);
            color: white;
        }
        .btn-clear:hover {
            background-color: var(--secondary-color);
        }
        
        /* --- Table Container --- */
        .table-container { 
            background: var(--white); 
            border-radius: 12px; 
            box-shadow: 0 5px 15px rgba(0,0,0,0.05); 
            overflow: hidden; 
            border: 1px solid var(--border-color);
        }
        table { 
            width: 100%; 
            border-collapse: collapse; 
        }

        /* --- Table Header --- */
        th { 
            background-color: #fafbfc; 
            color: var(--text-light);
            font-weight: 700;
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            padding: 20px 24px; 
            text-align: left;
        }
        
        /* --- Table Body --- */
        td {
            padding: 20px 24px;
            font-weight: 500;
            color: var(--text-dark);
            font-size: 0.9rem;
            text-align: left;
        }
        
        tbody tr {
            border-bottom: 1px solid var(--border-color);
            transition: background-color 0.2s ease;
        }
        
        tbody tr:last-child {
            border-bottom: none;
        }
        
        tbody tr:hover {
            background-color: #fcfcfc;
        }
        
        /* Style for N/A text */
        .na {
            color: var(--text-light);
            font-style: italic;
            font-weight: 400;
        }

        /* --- Alerts (From your provided code) --- */
        .alert {
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            font-size: 0.95rem;
        }
        .alert-error { /* Renamed from alert-danger */
            color: #721c24;
            background-color: #f8d7da;
            border: 1px solid #f5c6cb;
        }
        .alert-info {
            color: #004085;
            background-color: #cce5ff;
            border: 1px solid #b8daff;
        }
        .alert-info a { color: #004085; font-weight: 600; }

        /* --- Status Badges --- */
        .status-cell {
            text-transform: capitalize;
        }
        .status-badge {
            padding: 5px 12px;
            border-radius: 30px;
            font-weight: 700;
            font-size: 0.75rem;
            color: var(--white);
        }
        .status-completed {
            background-color: var(--success);
        }
        .status-approved {
            /* FIXED: Changed from red to blue */
            background-color: var(--info);
            color: var(--white);
        }
        .status-pending {
            background-color: var(--warning);
            color: var(--text-dark);
        }
        .status-ongoing {
            background-color: #6c757d; /* Neutral grey */
        }
        .status-rejected,
        .status-cancelled {
            background-color: var(--danger);
        }

        /* --- Responsive Design (Updated) --- */
        @media (max-width: 768px) {
            /* Stacks top-bar and main content vertically */
            body {
                flex-direction: column;
            }

            /* Converts sidebar to a horizontal top-bar */
            .sidebar {
                width: 100%;
                height: 60px; /* Fixed height for top bar */
                position: sticky; /* Sticks to the top */
                top: 0;
                flex-direction: row;
                align-items: center;
                justify-content: space-between; /* Logo left, toggle right */
                padding: 0 20px; /* Adjusted padding */
            }

            .sidebar h2 {
                margin-bottom: 0;
                font-size: 20px;
            }

            /* Show the toggle button */
            .menu-toggle {
                display: block;
            }

            /* This is the new link container */
            .sidebar-links {
                position: fixed;
                top: 60px; /* Position below the top bar */
                left: 0;
                width: 250px; /* Same width as desktop sidebar */
                height: calc(100vh - 60px); /* Full height minus top bar */
                background: var(--secondary-color);
                padding: 20px;
                
                /* Hide off-screen by default */
                transform: translateX(-100%);
                transition: transform 0.3s ease-in-out;
                
                /* Ensure links stack vertically */
                display: flex;
                flex-direction: column;
            }
            
            /* This class is added by JS to show the menu */
            .sidebar-links.open {
                transform: translateX(0);
            }

            /* Reset nav and logout styles for vertical menu */
            .sidebar-links nav {
                flex: 1;
            }
            .sidebar a {
                margin-bottom: 8px;
            }
            .sidebar a.logout {
                margin-top: 20px;
                margin-left: 0; /* Remove auto margin */
            }

            /* Removes the 250px margin so content takes full width */
            .main {
                margin-left: 0;
                padding: 20px;
            }
            
            .main h1 { font-size: 1.6rem; }

            /* Makes filter bar stack */
            .filter-bar {
                flex-direction: column;
                align-items: stretch; /* Make inputs full width */
            }
            .filter-buttons {
                justify-content: flex-start;
                margin-top: 5px;
            }

            /* This will make the table horizontally scrollable */
            .table-container {
                overflow-x: auto;
            }
            table {
                min-width: 700px; /* Force table to be wide */
            }
        }
    </style>
</head>
<body>

<div class="sidebar">
    <h2>SharingYatra</h2>

    <button id="menu-toggle" class="menu-toggle">
        <i class="fa-solid fa-bars"></i>
    </button>

    <div class="sidebar-links">
        <nav>
            <a href="/manageBooking"><i class="fa-solid fa-calendar-check"></i> Manage Bookings</a>
            <a href="/approvedRides" class="active"><i class="fa-solid fa-check-double"></i> Approved</a>
            <a href="/addDriver"><i class="fa-solid fa-user-plus"></i> Add Drivers</a>
            <a href="/viewDriver"><i class="fa-solid fa-users"></i> View Drivers</a>
            <a href="/addVehicles"><i class="fa-solid fa-car"></i> Add Vehicles</a>
            <a href="/viewVehicles"><i class="fa-solid fa-car-side"></i> View Vehicles</a>
            <a href="#"><i class="fa-solid fa-chart-line"></i> Earnings</a>
            <a href="/dashboard"><i class="fa-solid fa-user-circle"></i> Profile</a>
        </nav>
        <a href="/logout" class="logout"><i class="fa-solid fa-right-from-bracket"></i> Logout</a>
    </div>
</div>

<div class="main">
    <h1>View All Approved Rides</h1>
    
    <% if (error) { %>
        <div class="alert alert-error"><%= error %></div>
    <% } %>
<!-- 
    <form class="filter-bar" method="GET" action="/approvedRides">
        <div class="filter-group">
            <label for="dateFrom">From</label>
            <input 
              type="date" 
              id="dateFrom" 
              name="dateFrom"
              value="<%= (typeof query !== 'undefined' && query.dateFrom) ? query.dateFrom : '' %>">
        </div>
        <div class="filter-group">
            <label for="dateTo">To</label>
            <input 
              type="date" 
              id="dateTo" 
              name="dateTo"
              value="<%= (typeof query !== 'undefined' && query.dateTo) ? query.dateTo : '' %>">
        </div>
        <div class="filter-buttons">
            <button type="submit" class="btn btn-filter">
                <i class="fa-solid fa-filter"></i> Filter
            </button>
            <a href="/approvedRides" class="btn btn-clear">Clear</a>
        </div>
    </form> -->

    <% if (approvedList.length === 0 && !error) { %>
        <div class="alert alert-info">No approved rides found.</div>
    <% } else if (approvedList.length > 0) { %>
        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th>Customer Name</th>
                        <th>Phone Number</th>
                        <th>From</th>
                        <th>To</th>
                        <th>Ride Date</th>
                        <th>Driver Name</th>
                        <th>Fare (₹)</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                    <% approvedList.forEach(list => { %>
                        <tr>
                            <td><%= list.customerName || list.name %></td>
                            <td><%= list.mobile || '' %></td>
                            <td><%= list.from || '' %></td>
                            <td><%= list.to || '' %></td>
                            <td><%= new Date(list.date).toLocaleDateString('en-IN') %></td>
                            
                            <td><%- list.driverName || '<span class="na">N/A</span>' %></td>
                            
                            <td><%- list.fare ? '₹' + list.fare : '<span class="na">N/A</span>' %></td>
                            
                            <td class="status-cell">
                                <span class="status-badge status-<%= (list.status || 'pending').toLowerCase() %>">
                                    <%= list.status %>
                                </span>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    <% } %>
</div>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        // --- Sidebar Toggle Functionality ---
        document.getElementById('menu-toggle').addEventListener('click', function() {
            document.querySelector('.sidebar-links').classList.toggle('open');
        });
    });
</script>

</body>
</html>
